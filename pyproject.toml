[project]
name = "adventure-game"
version = "0.1.0"
description = ""
authors = [
    {name = "Scott Frasso", email = "scottfrasso@gmail.com"}
]
dependencies = [
    "instructor>=1.4.0",
    "openai>=1.43.0",
    "pydantic>=2.8.2",
    "python-dotenv>=1.0.1",
    "ruff>=0.6.3",
    "xmltodict>=0.13.0",
    "textual>=0.79.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["adventure_game"]
